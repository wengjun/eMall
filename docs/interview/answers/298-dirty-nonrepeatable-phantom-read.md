# 298 脏读、不可重复读、幻读是什么？

[返回按分类学习面试题](../README.md)

## 题目

脏读、不可重复读、幻读是什么？

## 先给面试官的短答案

脏读是读到了其他事务未提交的数据；不可重复读是同一事务两次读取同一行，结果不同；
幻读是同一事务两次按条件查询，第二次出现或消失了符合条件的行。

这些问题来自并发事务之间的可见性和隔离级别差异。

## 脏读

事务 A 修改数据但未提交。

事务 B 读到了这份未提交数据。

如果事务 A 回滚，事务 B 读到的就是不存在的脏数据。

## 不可重复读

事务 A 第一次读取订单状态为待支付。

事务 B 提交修改，把状态改为已支付。

事务 A 第二次读取同一订单，状态变成已支付。

## 幻读

事务 A 查询某用户待支付订单数量为 1。

事务 B 插入一条新的待支付订单并提交。

事务 A 再次按同样条件查询，结果变成 2，这条新增记录像幻影一样出现。

## 在 eMall 项目中怎么讲？

库存扣减不能依赖先查库存再扣减，因为并发事务可能在两次操作之间修改库存。

应使用条件更新或锁，保证并发扣减下库存不会变成负数。

## 深度增强：数据访问和扩展图

![数据库、缓存和消息一致性链路](../assets/data-cache-mq.svg)

数据库题要从访问路径、索引、锁、事务和容量出发。电商系统的数据层既要支撑高并发读写，
又要保证订单、库存、支付等事实数据可追踪。缓存和消息可以提升性能，但不能替代数据库事实来源。

## 深度增强：Java 17 数据访问策略示例

```java
record QueryPlan(String accessPath, boolean usesIndex, boolean requiresPagination) {

    boolean safeForOnlineTraffic() {
        return usesIndex && requiresPagination;
    }
}

final class OnlineQueryPolicy {

    void verify(QueryPlan plan) {
        if (!plan.safeForOnlineTraffic()) {
            throw new IllegalArgumentException("Online query must use index and pagination");
        }
    }
}
```

这段代码体现线上查询治理：不是 SQL 能跑就可以上线，而是要确认走索引、可分页、可限流、可观测。

## 深度增强：生产边界

核心表设计要从典型查询倒推索引，避免全表扫描、深分页和大事务。分库分表要先选好分片键，
避免跨分片事务和热点分片。任何数据迁移都要支持灰度、校验、回滚或修复。

## 深度增强：面试高分表达

我会从访问模式回答数据题：谁查、按什么条件查、QPS 多少、数据量多大、是否强一致、是否需要分页和排序。
然后再决定索引、分片、缓存、读写分离和归档策略。

## 专家级完整回答

```text
脏读是读到未提交数据；不可重复读是同一事务两次读同一行结果不同；
幻读是同一事务两次按条件查询，结果集合出现新增或消失的行。

这些问题由隔离级别控制。业务上不能只依赖读到的快照做关键写入，库存扣减等场景要用条件更新或锁保护。
```

## 回答评分点

高分答案应该覆盖：

- 脏读是读未提交数据。
- 不可重复读关注同一行变化。
- 幻读关注结果集变化。
- 问题来自并发事务隔离。
- 能结合库存或订单举例。
