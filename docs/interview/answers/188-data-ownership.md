# 188 什么是数据所有权？

[返回按分类学习面试题](../README.md)

## 题目

什么是数据所有权？

## 先给面试官的短答案

数据所有权是指某类业务数据只能由一个服务负责定义、写入、校验和发布变更。
其他服务不能直接修改这份数据，只能通过拥有者提供的 API、事件或只读投影使用。

没有数据所有权，微服务就会退化成多个应用共享一个数据库。

## 所有权包含什么？

包含：

- 表结构定义权。
- 写入和更新权。
- 业务规则校验权。
- 状态流转控制权。
- 数据质量责任。
- 对外契约发布权。
- 数据变更事件发布权。

拥有者不仅拥有数据，也承担数据正确性的责任。

## 为什么重要？

数据所有权能避免：

- 多服务同时写同一张表。
- 业务规则散落在多个服务。
- 状态被绕过非法修改。
- 表结构变更影响未知调用方。
- 故障时无法确定责任边界。

它是服务自治的基础。

## 使用他人数据的方式

其他服务可以：

- 调用 API 查询。
- 订阅领域事件。
- 构建本地只读投影。
- 通过数据平台做离线分析。

不能直接写对方数据库表。

## 在 eMall 项目中怎么讲？

订单数据归订单服务所有，库存数据归库存服务所有，支付单数据归支付服务所有。

推荐服务可以订阅订单完成事件训练模型，但不能直接更新订单表里的状态。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
数据所有权是微服务自治的基础。某类业务数据应由一个服务负责模型定义、写入、校验、状态流转和变更发布。
其他服务只能通过 API、事件或只读投影使用这些数据，不能直接写拥有者的表。

这样可以避免规则分散、状态被绕过、表结构变更失控和责任边界不清。
```

## 回答评分点

高分答案应该覆盖：

- 数据所有权包括写入、校验和变更责任。
- 一个业务数据应有明确拥有者。
- 其他服务不能直接写拥有者的表。
- API、事件和只读投影是常见使用方式。
- 数据所有权是微服务自治基础。
