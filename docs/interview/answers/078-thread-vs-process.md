# 078 线程和进程有什么区别？

[返回按分类学习面试题](../README.md)

## 题目

线程和进程有什么区别？

## 先给面试官的短答案

进程是操作系统资源分配的基本单位，拥有独立地址空间；线程是 CPU 调度的基本单位，同一进程内线程共享堆、
方法区、文件句柄等资源，但每个线程有自己的栈和程序计数器。进程隔离性强，线程通信成本低但共享状态更容易出并发问题。

Java 服务通常一个 JVM 进程内运行多个线程。

## 进程是什么？

进程可以理解为正在运行的程序实例。

它通常拥有：

- 独立虚拟地址空间。
- 文件句柄。
- 网络连接。
- 环境变量。
- 进程 ID。
- 操作系统资源配额。

一个 Java 微服务实例通常就是一个 JVM 进程。

## 线程是什么？

线程是进程内的执行流。

一个进程可以有多个线程。

每个线程有：

- 程序计数器。
- Java 虚拟机栈。
- 本地方法栈。
- 线程本地变量。

同一进程中的线程共享 heap、metaspace、打开的文件和连接等资源。

## 核心区别

| 对比项 | 进程 | 线程 |
| --- | --- | --- |
| 资源隔离 | 强 | 弱 |
| 地址空间 | 独立 | 同进程共享 |
| 创建成本 | 高 | 较低 |
| 通信方式 | IPC、网络 | 共享内存、队列 |
| 崩溃影响 | 通常影响自身 | 可能拖垮整个进程 |
| 并发风险 | 较低 | 共享状态竞争 |

## 为什么线程更容易出并发问题？

线程共享进程内存。

多个线程同时读写同一个对象，如果没有同步，就可能出现：

- 脏读。
- 丢失更新。
- 可见性问题。
- 指令重排序问题。
- 死锁。
- 竞态条件。

所以 Java 并发重点是治理共享状态。

## Java 中的线程

Java 中 `Thread` 对应 JVM 管理的线程，最终会映射到操作系统线程。

常见线程包括：

- main 线程。
- Web 请求线程。
- GC 线程。
- JIT 编译线程。
- MQ 消费线程。
- 定时任务线程。
- 业务线程池线程。

线程多了会增加调度和内存成本。

## 微服务和进程隔离

微服务通常把不同业务能力拆成不同进程或 Pod。

好处：

- 故障隔离。
- 独立扩容。
- 独立发布。
- 资源配额独立。
- 技术栈可演进。

代价是网络通信、分布式事务和运维复杂度增加。

## 在 eMall 项目中怎么讲？

eMall 的订单、库存、支付是不同微服务进程，进程之间通过 HTTP、RPC 或消息通信。

订单服务进程内部又有多个线程处理请求、调用下游、消费消息和执行定时任务。

服务拆分解决进程级隔离，线程池隔离解决进程内资源隔离，两者都需要设计。

## 深度增强：并发治理图

![Java 并发从线程安全到容量保护](../assets/concurrency-governance.svg)

并发题不能只回答 API 用法。生产系统要同时考虑线程安全、资源隔离、超时、拒绝、幂等和分布式多实例。
单机锁只能保护当前 JVM，不能保护整个集群；线程池满也不是小问题，而是容量和可用性风险。

## 深度增强：Java 17 有界并发示例

```java
import java.util.concurrent.Semaphore;
import java.util.function.Supplier;

final class BulkheadGuard {
    private final Semaphore permits;

    BulkheadGuard(int maxConcurrentCalls) {
        this.permits = new Semaphore(maxConcurrentCalls);
    }

    <T> T execute(Supplier<T> supplier) {
        if (!permits.tryAcquire()) {
            throw new IllegalStateException("Bulkhead rejected the call");
        }
        try {
            return supplier.get();
        } finally {
            permits.release();
        }
    }
}
```

这段代码展示了并发控制的生产思路：不是让所有请求无限进入系统，而是在入口保护共享资源。
真实项目还要加超时、指标、降级和按下游隔离。

## 深度增强：生产边界

线程安全不等于系统安全。`ConcurrentHashMap` 只能保护当前进程内的数据结构，
不能替代数据库唯一键、幂等表或分布式一致性。线程池也不能使用无界队列，
否则会把过载转化成内存上涨和 P99 恶化。

## 深度增强：面试高分表达

我会把并发问题分成三层：JMM 和锁保证单机正确性，线程池和隔离保证资源不被拖垮，
幂等和唯一键保证分布式正确性。这样能体现我理解 Java 并发，也理解微服务生产稳定性。

## 专家级完整回答

```text
进程是资源分配和隔离单位，拥有独立地址空间；线程是 CPU 调度单位，同一进程内线程共享 heap、
文件句柄和连接等资源，但每个线程有自己的栈和程序计数器。进程隔离强但通信成本高，线程通信方便
但共享状态会带来并发安全问题。

在 Java 微服务中，一个服务实例通常是一个 JVM 进程，进程内用多个线程池处理请求、MQ 和异步任务。
```

## 回答评分点

高分答案应该覆盖：

- 进程是资源分配单位。
- 线程是调度单位。
- 线程共享进程内存。
- 每个线程有自己的栈和程序计数器。
- 能联系 JVM 进程和微服务隔离。
