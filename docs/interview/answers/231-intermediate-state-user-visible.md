# 231 用户看到中间状态是否可接受？

[返回按分类学习面试题](../README.md)

## 题目

用户看到中间状态是否可接受？

## 先给面试官的短答案

是否可接受取决于业务语义。分布式系统中，中间状态很常见，但必须让用户看到可理解、可恢复、不会误导的状态。
例如“支付处理中”“退款处理中”可以接受，但“支付成功却显示订单不存在”不可接受。

中间状态要被产品化，而不是把系统内部不一致直接暴露给用户。

## 可接受的中间状态

常见可接受状态：

- 待支付。
- 支付处理中。
- 库存确认中。
- 退款处理中。
- 履约处理中。
- 售后审核中。

这些状态本身就是业务流程的一部分。

## 不可接受的中间状态

不可接受：

- 用户已付款但订单消失。
- 库存扣减成功但订单失败且不退款。
- 页面显示相互矛盾的状态。
- 用户无法知道下一步。
- 状态长期停留且无人处理。

不可接受的核心是用户权益不清晰或无法恢复。

## 设计要求

要求包括：

- 状态命名用户可理解。
- 给出下一步提示。
- 设置超时处理。
- 后台补偿可推进状态。
- 客服和运营可查询。
- 状态变化有通知。

每个中间状态都要有退出路径。

## 在 eMall 项目中怎么讲？

支付回调可能晚于用户页面刷新。

页面可以显示“支付结果确认中”，并提示稍后刷新。后台通过支付回调、主动查询和对账把订单推进到已支付或支付失败。

## 深度增强：一致性和补偿图

![交易一致性、对账和补偿闭环](../assets/consistency-compensation-loop.svg)

分布式一致性题要先区分事实来源、状态流转和补偿责任。
订单、库存、支付、优惠和消息不可能总靠一个本地事务完成，
所以要用幂等、状态机、Outbox、重试、对账和补偿形成闭环。

## 深度增强：Java 17 状态机示例

```java
enum TradeState {
    INIT,
    RESERVED,
    PAID,
    CLOSED
}

record TradeTransition(TradeState from, TradeState to, String reason) {

    boolean valid() {
        return switch (from) {
            case INIT -> to == TradeState.RESERVED || to == TradeState.CLOSED;
            case RESERVED -> to == TradeState.PAID || to == TradeState.CLOSED;
            case PAID, CLOSED -> false;
        };
    }
}
```

状态机的价值是防止非法跳转。生产事故中很多错误不是技术异常，而是状态被重复推进、逆向推进或越级推进。

## 深度增强：生产边界

最终一致不是“最终随便一致”。每个异步环节都要有唯一业务键、幂等处理、重试策略、死信、补偿任务和对账报表。
涉及资金和库存时，宁可慢一点，也要保证事实可追踪、可审计、可修复。

## 深度增强：面试高分表达

我会先承认分布式系统无法用一个本地事务覆盖所有服务，再说明如何把不确定性收敛：
本地事务写事实和 Outbox，消费者幂等处理，失败进入重试和死信，后台对账发现差异并补偿。

## 专家级完整回答

```text
用户是否能看到中间状态取决于业务是否能解释和恢复。像支付处理中、退款处理中是合理业务状态；
但支付成功却订单不存在这种矛盾状态不可接受。

我会把中间状态产品化，给用户明确提示和下一步，同时用状态机、超时任务、补偿和对账确保状态最终退出。
```

## 回答评分点

高分答案应该覆盖：

- 中间状态在分布式系统中常见。
- 是否可见取决于业务语义。
- 状态要用户可理解。
- 每个中间状态要有退出路径。
- 补偿和对账保证最终收敛。
