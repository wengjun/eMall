# 203 核心链路和非核心链路如何隔离？

[返回逐题精讲目录](README.md) | [返回答案手册](../README.md)

完成标记：已完成

深度完善标记：已完成

## 题目

核心链路和非核心链路如何隔离？

## 先给面试官的短答案

核心链路隔离的目标是让推荐、广告、通知、报表等非核心能力故障时，不影响下单、支付、库存等核心交易。
常用手段包括服务分级、线程池隔离、连接池隔离、限流、熔断、降级、异步化、数据隔离和资源配额。

隔离不是只在代码里判断异常，而是从运行资源到业务流程都要隔离。

## 隔离维度

维度包括：

- 服务依赖隔离。
- 线程池隔离。
- 连接池隔离。
- 缓存和数据库资源隔离。
- MQ topic 和消费组隔离。
- 部署集群隔离。
- 配置和开关隔离。
- 告警和值班隔离。

共享资源越多，故障越容易传播。

## 主链路治理

核心链路要：

- 减少同步依赖。
- 明确强依赖和弱依赖。
- 弱依赖必须可降级。
- 下游调用设置超时预算。
- 失败快速返回或使用兜底。
- 禁止大查询和慢任务抢占资源。

核心资源应优先保障交易成功。

## 非核心处理方式

非核心能力可以：

- 异步消费事件。
- 使用独立线程池。
- 使用独立队列。
- 使用缓存默认值。
- 失败后补偿。
- 高峰期主动降级。

非核心链路要接受延迟和降级。

## 在 eMall 项目中怎么讲？

下单接口可以同步依赖库存和价格，但不应同步依赖短信、推荐刷新和数据仓库写入。

短信失败不能导致下单失败，推荐失败可以隐藏模块，数据仓库写入失败可以通过 MQ 重试。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
核心链路和非核心链路要从调用、资源和业务语义上隔离。核心链路只保留必要强依赖，并设置超时、熔断和降级；
非核心能力通过事件异步处理，使用独立线程池、队列、连接池和资源配额。

目标是非核心服务故障、积压或慢查询时，不会抢占核心交易资源，也不会让用户下单和支付失败。
```

## 回答评分点

高分答案应该覆盖：

- 隔离要覆盖调用和资源。
- 核心链路要控制同步依赖。
- 弱依赖必须可降级。
- 非核心能力应异步化。
- 线程池、连接池、队列和数据资源都要隔离。

## 深度完善：面向 L6 的回答框架

围绕「核心链路和非核心链路如何隔离？」，高分答案不能停在概念定义，而要把「服务拆分、数据归属、依赖图、调用链、团队边界和演进策略」讲成一条可验证的工程链路。
面试官真正关注的是：你是否知道它解决什么问题、什么时候会失效、如何在生产系统中验证。

### 1. 先界定边界

- 本题属于「微服务边界和架构治理」，先说明它影响的是正确性、稳定性、性能、安全还是协作效率。
- 不要直接背结论，要先说清业务约束、数据规模、调用链位置和失败后果。
- 如果存在多种方案，要说明默认选择、替代方案、迁移成本和放弃条件。

### 2. 结合 eMall 落地

- 可以从 `user、product、order、inventory、payment、fulfillment、search、risk 的边界` 切入，说明它在真实电商链路中的入口、状态、数据和依赖。
- 回答时至少补一个失败路径，例如超时、重复请求、状态不一致、热点流量或配置误发。
- 再说明如何通过代码规范、测试、灰度、回滚、监控或补偿把风险收敛。

### 3. 生产级验证

- 关键指标：跨服务调用深度、依赖数、变更耦合度、故障半径、跨团队交付周期。
- 验证证据：服务依赖图、ADR、契约测试、发布记录、事故复盘和边界评审结论。
- 如果没有这些证据，只能说明方案在理论上成立，不能证明它能长期稳定运行。

### 4. 追问防守

- 被问“为什么不用更简单方案”时，回答当前规模、团队能力和风险收益是否匹配。
- 被问“为什么不用更复杂方案”时，回答复杂方案的运维成本、故障面和迁移成本。
- 最后用一句话收束：先用简单可靠方案闭环，再用指标驱动演进，而不是提前复杂化。

## 补强索引

重复补强内容已合并到 [面试补强共享框架](../shared/deepening-framework.md)。

整理标记：重复内容已合并

本题复习重点：核心链路和非核心链路如何隔离？

- 先看本文的题目专属答案，再按共享框架补齐项目落点、失败路径、取舍和验收。
- 白板复述时用结论 -> 例子 -> 风险 -> 指标四层结构。
