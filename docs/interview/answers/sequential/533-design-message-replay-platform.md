# 533 设计消息重放平台

[返回逐题精讲目录](README.md) | [返回答案手册](../README.md)

完成标记：已完成

## 题目

设计消息重放平台。

## 先给面试官的短答案

消息重放平台用于在消费失败、数据修复或业务补偿时重新投递历史消息。核心要求是可筛选、可审批、可限速、
可幂等、可审计和可停止。重放不是简单把 Kafka offset 往回拨，必须控制影响范围和业务副作用。

## 使用场景

常见场景包括消费者 bug 修复后重放失败消息、数据仓库漏消费补数据、履约事件未处理、搜索索引缺数据、
以及 Outbox 事件需要重新发送。

不同场景的风险不同。交易类消息重放必须非常谨慎，分析类消息重放风险较低。

## 平台能力

平台要支持按 topic、时间、key、eventId、订单号、状态和错误类型筛选消息。重放前展示影响范围和样本。

重放任务要支持审批、限速、暂停、恢复、停止、进度查看和失败重试。

所有操作要记录操作者、原因、筛选条件、消息数量、开始结束时间和结果。

## 幂等和安全

消费者必须幂等。重放平台要提醒或校验目标消费者是否支持幂等，尤其是履约、发券、退款和库存类消息。

对高风险 topic，可以要求双人审批、只允许重放到隔离 topic，或者先 dry run。

## 在 eMall 项目中怎么讲？

eMall 的 `event-platform` 可以提供消息检索和重放，`operations` 提供审批后台。
订单、支付、库存、履约的消息重放要受严格权限和限速控制。

## 深度增强：系统设计落地图

![京东/Amazon 类电商总体架构](../../assets/ecommerce-architecture.svg)

系统设计题要从目标、约束、流量、数据、核心链路、故障模式和演进路线回答。
电商系统不是模块堆叠，而是订单、库存、支付、商品、搜索、推荐、风控、履约和运营治理共同形成的业务闭环。

## 深度增强：Java 17 设计取舍模型

```java
record DesignTradeoff(String option, String benefit, String cost, String risk) {

    String summary() {
        return option + " improves " + benefit + ", costs " + cost + ", risks " + risk;
    }
}
```

这个模型体现系统设计回答方法：不要只说“用某技术”，还要说收益、成本、风险和适用条件。

## 深度增强：生产边界

生产级系统要考虑容量、数据一致性、灰度发布、容灾、可观测性、安全、成本和团队运维能力。
如果某个方案无法监控、无法回滚、无法对账或无法补偿，就不能算完整设计。

## 深度增强：面试高分表达

我会先定义 SLO 和业务目标，再拆核心链路和数据流，最后讨论瓶颈、故障、降级、扩容和演进。
这样能体现架构判断力，而不是简单罗列中间件。

## 专家级完整回答

```text
消息重放平台的价值是把消息修复从手工脚本变成受控流程。

我会支持按条件筛选消息、创建重放任务、审批、限速、暂停、恢复和审计。
重放前必须评估消费者幂等性和业务副作用，不能简单回拨 offset。

对核心交易消息，重放要有 dry run、双人审批和严格限速。否则一次错误重放可能造成重复发货、
重复发券或错误状态写入。
```

## 回答评分点

高分答案应该覆盖：

- 说明重放用于失败修复和数据补偿。
- 覆盖筛选、审批、限速、暂停、恢复和审计。
- 知道不能简单回拨 offset。
- 强调消费者幂等和业务副作用。
- 能结合订单、履约、搜索索引等场景。

## 二次深度补强

题目：设计消息重放平台。

二次补强标记：已完成

### 面试官真正想确认的能力

业务设计题要覆盖领域边界、核心链路、数据一致性、容量和商业指标。
围绕这道题，要进一步把概念、项目实现、线上风险和验证闭环连起来。

### 深度和广度补充

- 先画清用户路径、商家路径、运营路径和平台治理路径。
- 再拆核心领域：商品、价格、库存、交易、支付、履约和售后。
- 随后说明高峰流量、热点 SKU、活动规则、对账和补偿。
- 最后说明平台能力如何复用，而不是每个业务重复造轮子。

### 图片讲解

![二次补强图解](../../assets/ecommerce-architecture.svg)

- 图中从入口流量到商品、交易、支付、履约、数据平台逐层展开。
- 读图时要说明同步链路要短，异步链路要可靠，治理能力要平台化。
- 高分回答要体现你能从业务目标倒推技术架构。

### Java17 应用服务编排示例

```java
public record CheckoutCommand(long userId, long skuId, int quantity) {
}

public interface InventoryPort {
    boolean reserve(long skuId, int quantity);
}

final class CheckoutApplicationService {
    private final InventoryPort inventory;

    CheckoutApplicationService(InventoryPort inventory) {
        this.inventory = inventory;
    }

    boolean checkout(CheckoutCommand command) {
        return inventory.reserve(command.skuId(), command.quantity());
    }
}
```

### 高分表达要点

- 不要只回答定义，要说明为什么这样设计、在什么条件下失效、如何监控和回滚。
- 把答案和当前电商项目联系起来，例如订单、库存、支付、履约、搜索、风控或发布链路。
- 主动给出边界条件和反例，能让面试官看到你具备生产系统判断力。

## 逐题专项补强

逐题专项补强标记：已完成

### 本题专项切入

- 本题要围绕「设计消息重放平台。」展开，不要只复述分类模板。
- 先澄清目标和约束，再给出架构、数据流、容量和失败路径。
- 回答要同时覆盖当前实现、扩展路线、成本和风险。

### 专项图解说明

![逐题专项图解](../../assets/kafka-retry-dlq.svg)

- 这张图用于把「设计消息重放平台。」放回生产链路中理解，重点看入口、状态、数据和恢复闭环。
- 面试时可以先按图说明主路径，再补失败路径、监控指标和回滚手段。

### 贴合本题的实现示例

```java
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

final class MessageDeduplicator {
    private final Set<String> consumed = ConcurrentHashMap.newKeySet();

    boolean shouldConsume(String messageId) {
        return consumed.add(messageId);
    }
}
```

### 进一步追问时的回答边界

- 如果面试官继续追问，要主动说明这个实现是核心模型，不等于完整生产组件。
- 生产级落地还需要接入鉴权、幂等、限流、熔断、监控、告警、灰度和数据修复。
- 回答时把复杂度、失败场景、验证方式和 eMall 项目中的落地位置一起说清楚。

## 面试实战补强

面试实战补强标记：已完成

### 面试追问路线

- 这个设计的目标、非目标、容量、可用性和一致性要求是什么？
- 正常路径之外，失败、超时、重试、回滚、对账和补偿怎么处理？
- 如果规模扩大十倍，架构瓶颈和演进顺序是什么？

### eMall 项目落点

- 可以落到模块：event-platform、order、payment、fulfillment。
- 回答「设计消息重放平台。」时，要从这些模块里选一个主链路做例子。
- 讲清入口、状态变化、数据写入、异步事件、失败补偿和观测指标。

### 生产验证指标

- 消费滞后量
- 重复消费拦截数
- 死信数量
- 端到端投递时延

### 低分陷阱

- 只背定义，不说明业务场景和失败场景。
- 只讲正常路径，不讲超时、重试、回滚、补偿和监控。
- 只给方案，不给验证指标和取舍边界。

### 30 秒高分收束

这道题我会用 业务系统设计和平台设计 的视角回答。
先给结论，再给项目例子，然后补失败场景、验证指标和取舍边界。
这样能让面试官看到我不是只会背知识点，而是能把知识点落到生产系统。

## 架构取舍与反驳补强

架构取舍补强标记：已完成

### 先给立场

- 回答「设计消息重放平台。」时，不能只给单一方案，要先说明约束、目标和失败边界。
- 高分回答要让面试官看到你能在正确性、可用性、成本、复杂度和团队能力之间做判断。

### 可选方案对比

- 本地事务加 Outbox：可靠性高，延迟和存储成本会增加。
- 直接发 MQ：实现简单，但本地事务和消息发送之间存在不一致窗口。
- TCC 或 Saga：业务可控性强，但侵入性和状态管理复杂度更高。

### 反驳和防守

- 如果面试官问为什么不直接上最复杂方案，可以回答：复杂方案只有在规模和风险证明必要时才值得引入。
- 如果面试官问为什么不用最简单方案，可以回答：简单方案可以做第一期，但必须提前设计观测和迁移边界。
- 我的判断原则是：如果约束不明确，先补齐规模、延迟、可用性、一致性、成本和团队能力，再做选择。

### 决策证据

- 状态机和幂等记录
- 对账差异趋势
- 补偿任务成功率
- 核心链路错误率

### 一句话总结

我会先用简单可靠的方案解决当前确定性问题，同时保留观测、灰度和迁移能力。
当指标证明瓶颈存在，再演进到更复杂的架构，而不是为了显得高级提前复杂化。

## 生产落地验收补强

生产验收补强标记：已完成

### 上线前检查

- 针对「设计消息重放平台。」，先确认它影响的是正确性、稳定性、性能、安全还是成本。
- 确认消息唯一键、消费幂等、死信处理、补偿任务和重放脚本。
- 验收必须覆盖重复、乱序、积压、消费失败和人工重放。

### 灰度和回滚

- 先在测试环境和影子流量中验证，再做 1%、5%、25%、50%、100% 分阶段灰度。
- 每个阶段都设置自动暂停条件和人工回滚负责人。
- 回滚不是只回代码，还要确认配置、数据、缓存、消息和任务状态能一起回到安全状态。

### 监控和验收证据

- 幂等测试通过
- 补偿任务可重复执行
- 对账差异可解释
- 灰度期间核心链路无新增错误

### 面试表达

我不会只说方案能实现，还会说明上线前怎么验收、上线中怎么看指标、出问题怎么回滚。
这能证明我关注的是长期稳定运行，而不是只完成一次功能开发。

## 规模化与成本治理补强

规模成本补强标记：已完成

### 规模化视角

- 回答「设计消息重放平台。」时，要主动放到 10 亿用户、1 亿 DAU、100W 峰值并发的背景下思考。
- 按写入 TPS、消息大小、保留周期、消费并发和重放速度估算集群容量。
- 异步链路必须能在积压后追平，否则削峰会变成延迟债务。

### 成本治理

- 控制消息大小、保留周期、重试次数、死信堆积和跨机房复制成本。
- 对低价值事件做采样、合并或降级，而不是全部永久保存。

### 自动化和 owner

- 补偿、对账、死信重放和幂等校验都要任务化、可观测、可重复执行。
- 人工介入只能兜底，不能成为常态运维手段。

### 面试表达

我会补一句：方案能跑只是第一步，大规模下还要回答容量怎么估、成本怎么控、故障谁负责。
这能体现我不是只会实现单点功能，而是能长期运营一个高并发业务系统。

