# 107 分段锁和库存桶有什么相似点？

[返回逐题精讲目录](README.md) | [返回答案手册](../README.md)

完成标记：已完成

深度完善标记：已完成

## 题目

分段锁和库存桶有什么相似点？

## 先给面试官的短答案

分段锁和库存桶的共同思想都是把一个热点资源拆成多个较小单元，让并发请求分散到不同段或桶上，
降低单点竞争。分段锁拆的是锁竞争，库存桶拆的是库存热点行或库存计数竞争。

本质都是用空间和复杂度换并发能力。

## 分段锁是什么？

分段锁是把一把全局锁拆成多把锁。

例如按 key 哈希：

```text
segment = hash(key) % segmentCount
```

不同 key 落到不同 segment，可以并发执行。

只有落到同一 segment 的请求才互相竞争。

## 库存桶是什么？

库存桶是把一个商品总库存拆成多个桶。

例如商品库存 1000，拆成 10 桶，每桶 100。

扣减时选择一个桶扣减，减少所有请求竞争同一行或同一 key。

适合热点商品秒杀。

## 相似点

相似点：

- 都是拆热点。
- 都降低单点竞争。
- 都提高并发度。
- 都需要路由规则。
- 都增加合并和一致性复杂度。

核心思想是“不要让所有请求挤在同一个点上”。

## 不同点

分段锁主要是并发控制手段。

库存桶是业务数据建模手段。

分段锁关注锁冲突。

库存桶还要关注：

- 总库存准确。
- 桶间均衡。
- 售罄判断。
- 回滚和补偿。
- 数据一致性。

库存桶比普通分段锁业务复杂度更高。

## 库存桶风险

库存桶要处理：

- 某些桶提前扣完。
- 总库存还有但选中桶没库存。
- 桶间不均衡。
- 退款回补到哪个桶。
- 对账和汇总。
- 防止超卖。

不能只拆桶，不设计一致性。

## 在 eMall 项目中怎么讲？

秒杀商品可以把库存按桶拆分到多个 Redis key 或数据库行。

请求按用户 ID 或随机策略选择桶扣减。

这样能降低单 key 热点，但最终仍要有总库存校验、售罄标记、补偿和对账。

## 深度增强：并发治理图

![Java 并发从线程安全到容量保护](../../assets/concurrency-governance.svg)

并发题不能只回答 API 用法。生产系统要同时考虑线程安全、资源隔离、超时、拒绝、幂等和分布式多实例。
单机锁只能保护当前 JVM，不能保护整个集群；线程池满也不是小问题，而是容量和可用性风险。

## 深度增强：Java 17 有界并发示例

```java
import java.util.concurrent.Semaphore;
import java.util.function.Supplier;

final class BulkheadGuard {
    private final Semaphore permits;

    BulkheadGuard(int maxConcurrentCalls) {
        this.permits = new Semaphore(maxConcurrentCalls);
    }

    <T> T execute(Supplier<T> supplier) {
        if (!permits.tryAcquire()) {
            throw new IllegalStateException("Bulkhead rejected the call");
        }
        try {
            return supplier.get();
        } finally {
            permits.release();
        }
    }
}
```

这段代码展示了并发控制的生产思路：不是让所有请求无限进入系统，而是在入口保护共享资源。
真实项目还要加超时、指标、降级和按下游隔离。

## 深度增强：生产边界

线程安全不等于系统安全。`ConcurrentHashMap` 只能保护当前进程内的数据结构，
不能替代数据库唯一键、幂等表或分布式一致性。线程池也不能使用无界队列，
否则会把过载转化成内存上涨和 P99 恶化。

## 深度增强：面试高分表达

我会把并发问题分成三层：JMM 和锁保证单机正确性，线程池和隔离保证资源不被拖垮，
幂等和唯一键保证分布式正确性。这样能体现我理解 Java 并发，也理解微服务生产稳定性。

## 专家级完整回答

```text
分段锁和库存桶的共同点是把单热点拆成多个段或桶，让请求分散竞争，用空间和复杂度换并发能力。
分段锁拆的是锁，库存桶拆的是业务库存热点。

区别是库存桶还要保证总库存准确、桶间均衡、退款回补、售罄判断和对账，不能只做技术拆分而忽略业务一致性。
```

## 回答评分点

高分答案应该覆盖：

- 都是拆热点。
- 都降低单点竞争。
- 分段锁是并发控制。
- 库存桶是业务数据拆分。
- 库存桶要处理一致性和对账。

## 深度完善：面向 L6 的回答框架

围绕「分段锁和库存桶有什么相似点？」，高分答案不能停在概念定义，而要把「线程安全、锁、CAS、线程池、隔离、超时和并发容量」讲成一条可验证的工程链路。
面试官真正关注的是：你是否知道它解决什么问题、什么时候会失效、如何在生产系统中验证。

### 1. 先界定边界

- 本题属于「并发和线程治理」，先说明它影响的是正确性、稳定性、性能、安全还是协作效率。
- 不要直接背结论，要先说清业务约束、数据规模、调用链位置和失败后果。
- 如果存在多种方案，要说明默认选择、替代方案、迁移成本和放弃条件。

### 2. 结合 eMall 落地

- 可以从 `order 创建、inventory 扣减、payment 回调、outbox relay 和异步补偿任务` 切入，说明它在真实电商链路中的入口、状态、数据和依赖。
- 回答时至少补一个失败路径，例如超时、重复请求、状态不一致、热点流量或配置误发。
- 再说明如何通过代码规范、测试、灰度、回滚、监控或补偿把风险收敛。

### 3. 生产级验证

- 关键指标：线程池活跃数、队列长度、拒绝数、锁等待、超时率、重复请求数。
- 验证证据：并发单测、压测曲线、线程 dump、拒绝日志、幂等记录和容量评估。
- 如果没有这些证据，只能说明方案在理论上成立，不能证明它能长期稳定运行。

### 4. 追问防守

- 被问“为什么不用更简单方案”时，回答当前规模、团队能力和风险收益是否匹配。
- 被问“为什么不用更复杂方案”时，回答复杂方案的运维成本、故障面和迁移成本。
- 最后用一句话收束：先用简单可靠方案闭环，再用指标驱动演进，而不是提前复杂化。

## 补强索引

重复补强内容已合并到 [面试补强共享框架](../shared/deepening-framework.md)。

整理标记：重复内容已合并

本题复习重点：分段锁和库存桶有什么相似点？

- 先看本文的题目专属答案，再按共享框架补齐项目落点、失败路径、取舍和验收。
- 白板复述时用结论 -> 例子 -> 风险 -> 指标四层结构。
