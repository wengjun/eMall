# 230 最终一致和强一致如何取舍？

[返回按分类学习面试题](../README.md)

## 题目

最终一致和强一致如何取舍？

## 先给面试官的短答案

强一致适合资金、库存扣减、权限、唯一约束等不能出错的局部关键点。
最终一致适合跨服务长流程、通知、搜索索引、推荐、报表和可补偿业务。

取舍要看业务容忍度、用户体验、吞吐要求、故障恢复能力和实现成本。

## 强一致适合什么？

适合：

- 单账户余额更新。
- 支付状态确认。
- 库存不能超卖的关键扣减。
- 唯一订单号。
- 权限授权结果。

这些场景错误成本高，通常要在单服务或单数据库边界内保证。

## 最终一致适合什么？

适合：

- 搜索索引更新。
- 推荐特征更新。
- 积分到账。
- 站内信通知。
- 数据仓库入仓。
- 跨服务订单履约流程。

这些场景可以接受短暂延迟，但必须可追踪和可修复。

## 取舍问题

要问：

- 用户能否接受中间状态？
- 失败后能否补偿？
- 错误是否涉及资金或合规？
- 强一致的吞吐成本是否可接受？
- 是否能把强一致限制在局部？

很多系统不是全强一致或全最终一致，而是混合使用。

## 在 eMall 项目中怎么讲？

支付单状态和金额核对要强一致，不能同一笔支付出现两个最终金额。

订单支付成功后搜索索引更新可以最终一致，短时间内搜索不到新状态可以接受。

## 深度增强：一致性和补偿图

![交易一致性、对账和补偿闭环](../assets/consistency-compensation-loop.svg)

分布式一致性题要先区分事实来源、状态流转和补偿责任。
订单、库存、支付、优惠和消息不可能总靠一个本地事务完成，
所以要用幂等、状态机、Outbox、重试、对账和补偿形成闭环。

## 深度增强：Java 17 状态机示例

```java
enum TradeState {
    INIT,
    RESERVED,
    PAID,
    CLOSED
}

record TradeTransition(TradeState from, TradeState to, String reason) {

    boolean valid() {
        return switch (from) {
            case INIT -> to == TradeState.RESERVED || to == TradeState.CLOSED;
            case RESERVED -> to == TradeState.PAID || to == TradeState.CLOSED;
            case PAID, CLOSED -> false;
        };
    }
}
```

状态机的价值是防止非法跳转。生产事故中很多错误不是技术异常，而是状态被重复推进、逆向推进或越级推进。

## 深度增强：生产边界

最终一致不是“最终随便一致”。每个异步环节都要有唯一业务键、幂等处理、重试策略、死信、补偿任务和对账报表。
涉及资金和库存时，宁可慢一点，也要保证事实可追踪、可审计、可修复。

## 深度增强：面试高分表达

我会先承认分布式系统无法用一个本地事务覆盖所有服务，再说明如何把不确定性收敛：
本地事务写事实和 Outbox，消费者幂等处理，失败进入重试和死信，后台对账发现差异并补偿。

## 专家级完整回答

```text
强一致用于错误成本极高的局部关键点，例如资金、库存、权限和唯一约束。最终一致用于跨服务长流程、
可补偿和可延迟的场景，例如搜索索引、推荐、通知、积分和数据分析。

我会尽量把强一致限制在单服务或单库边界内，跨服务通过事件、状态机、补偿和对账实现最终一致。
```

## 回答评分点

高分答案应该覆盖：

- 强一致适合高风险关键点。
- 最终一致适合可补偿和可延迟场景。
- 取舍要看业务容忍度和成本。
- 强一致最好限制在局部。
- 大型系统通常混合使用两者。
