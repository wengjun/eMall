# 440 订单成功率下降如何排查？

[返回按分类学习面试题](../README.md)

完成标记：已完成

深度完善标记：已完成

## 题目

订单成功率下降如何排查？

## 先给面试官的短答案

订单成功率下降要先确认影响范围和下降阶段，是创建订单失败、库存失败、价格校验失败、风控拒绝、
支付预创建失败，还是超时导致用户放弃。然后按时间线关联发布、流量、依赖、错误码和业务指标。

核心是先拆漏斗，再定位具体环节。

## 先看范围

先确认：

- 全站还是部分接口。
- 全用户还是部分渠道。
- 所有商品还是部分 SKU。
- 所有商家还是部分商家。
- 是否和发布或活动时间重合。

范围决定排查方向。

## 拆交易漏斗

拆分：

- 商品详情访问。
- 加购。
- 结算。
- 提交订单。
- 库存预占。
- 价格校验。
- 风控校验。
- 支付单创建。

每一段都要有转化和失败原因。

## 技术排查

检查：

- 订单服务错误率。
- 库存服务错误率。
- 价格服务延迟。
- 风控拒绝率。
- 数据库慢 SQL。
- Redis 或 MQ 异常。
- 线程池和连接池。

成功率下降可能来自业务拒绝，也可能来自技术故障。

## 在 eMall 项目中怎么讲？

eMall 下单成功率下降时，先按错误码看失败集中在库存不足、价格变更、风控拒绝还是系统异常。

如果某些热点 SKU 库存预占失败率升高，排查库存桶、Redis 令牌、数据库行锁和秒杀流量隔离。

## 深度增强：Kubernetes 运维治理图

![Kubernetes 生产运行和故障治理](../assets/kubernetes-operations.svg)

Kubernetes 题不能只背 Deployment、Service 和 Ingress。生产稳定性还取决于资源 requests/limits、探针、HPA、PDB、
灰度发布、配置回滚、日志指标 Trace 和故障 Runbook。

## 深度增强：Java 17 发布门禁示例

```java
record ReleaseSignal(double errorRate, long p99Millis, double cpuThrottleRate, boolean rollbackSafe) {

    boolean canContinue() {
        return errorRate < 0.001
                && p99Millis < 300
                && cpuThrottleRate < 0.05
                && rollbackSafe;
    }
}
```

这段代码表达发布平台的核心：放量不是人工拍脑袋，而是由错误率、延迟、资源和回滚安全共同决定。

## 深度增强：生产边界

K8s 会重启失败容器，但不保证业务一定恢复。错误的 liveness probe 可能造成重启风暴；
过低的 CPU limit 会造成 throttling；不兼容数据库变更会让回滚失效。平台能力要和应用设计配合。

## 深度增强：面试高分表达

我会把 K8s 视为运行平台，而不是稳定性的全部答案。真正生产级要有容量规划、灰度门禁、配置治理、可观测性、
自动回滚和数据库兼容检查，才能支撑核心交易链路。

## 专家级完整回答

```text
订单成功率下降要先拆漏斗，确认失败发生在提交订单、库存、价格、风控、订单落库还是支付预创建。
同时按用户、渠道、SKU、商家和地域拆分影响范围，并关联发布和活动时间线。

排查时结合业务错误码、RED 指标、Trace、日志、数据库、Redis、MQ 和下游依赖。不要一上来只看
订单服务本身。
```

## 回答评分点

高分答案应该覆盖：

- 先确认影响范围。
- 拆交易漏斗。
- 按错误码分类。
- 关联发布、流量和依赖。
- 能结合库存、价格、风控排查。

## 深度完善：面向 L6 的回答框架

围绕「订单成功率下降如何排查？」，高分答案不能停在概念定义，而要把「日志、指标、Trace、SLO、告警、Runbook、事故复盘和故障演练」讲成一条可验证的工程链路。
面试官真正关注的是：你是否知道它解决什么问题、什么时候会失效、如何在生产系统中验证。

### 1. 先界定边界

- 本题属于「可观测性和 SRE」，先说明它影响的是正确性、稳定性、性能、安全还是协作效率。
- 不要直接背结论，要先说清业务约束、数据规模、调用链位置和失败后果。
- 如果存在多种方案，要说明默认选择、替代方案、迁移成本和放弃条件。

### 2. 结合 eMall 落地

- 可以从 `observability、analytics、operations、gateway、order、payment 的核心看板` 切入，说明它在真实电商链路中的入口、状态、数据和依赖。
- 回答时至少补一个失败路径，例如超时、重复请求、状态不一致、热点流量或配置误发。
- 再说明如何通过代码规范、测试、灰度、回滚、监控或补偿把风险收敛。

### 3. 生产级验证

- 关键指标：RED/USE 指标、SLO、错误预算、告警准确率、MTTD、MTTR。
- 验证证据：Trace 样例、指标看板、告警规则、Runbook、事故时间线和复盘行动项。
- 如果没有这些证据，只能说明方案在理论上成立，不能证明它能长期稳定运行。

### 4. 追问防守

- 被问“为什么不用更简单方案”时，回答当前规模、团队能力和风险收益是否匹配。
- 被问“为什么不用更复杂方案”时，回答复杂方案的运维成本、故障面和迁移成本。
- 最后用一句话收束：先用简单可靠方案闭环，再用指标驱动演进，而不是提前复杂化。

## 补强索引

重复补强内容已合并到 [面试补强共享框架](../deepening-framework.md)。

整理标记：重复内容已合并

本题复习重点：订单成功率下降如何排查？

- 先看本文的题目专属答案，再按共享框架补齐项目落点、失败路径、取舍和验收。
- 白板复述时用结论 -> 例子 -> 风险 -> 指标四层结构。
