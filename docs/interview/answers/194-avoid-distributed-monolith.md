# 194 如何避免分布式单体？

[返回按分类学习面试题](../README.md)

## 题目

如何避免分布式单体？

## 先给面试官的短答案

分布式单体是代码拆成多个服务，但数据、发布、调用和变更仍高度耦合。
避免它要从业务边界、数据所有权、接口契约、独立部署、依赖治理和组织责任上同时治理。

如果一个需求必须同时改十个服务，这通常不是微服务成功，而是边界失败。

## 分布式单体的表现

表现包括：

- 多服务共享数据库。
- 服务间频繁同步调用。
- 一个需求跨多个服务联动修改。
- 接口没有稳定契约。
- 发布必须按固定顺序。
- 测试必须启动全量系统。
- 团队职责边界不清。

这些问题会让系统比单体更难维护。

## 避免方式

关键做法：

- 按业务域拆分服务。
- 明确数据所有权。
- 禁止跨服务共享写表。
- 使用稳定 API 和事件契约。
- 控制同步依赖。
- 非核心流程异步解耦。
- 每个服务可以独立测试和部署。
- 服务团队对运行质量负责。

架构边界和组织边界要匹配。

## 何时回退？

如果两个服务总是一起改、一起发布、共用数据模型，说明边界可能不合理。

可以考虑：

- 合并服务。
- 重划边界。
- 抽取公共领域能力。
- 建立读模型而不是同步穿透。

合并错误拆分不是倒退，而是治理。

## 在 eMall 项目中怎么讲？

订单和库存可以独立，但不能让订单服务直接写库存表，也不能让库存服务直接改订单状态。

它们通过 API 和事件协作，各自维护自己的状态机和数据所有权。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
避免分布式单体要确保服务不仅代码独立，而且数据、发布、运行和责任也独立。
服务边界要按业务域划分，数据有明确拥有者，服务之间通过稳定 API 和事件协作，不能共享写表和频繁联动发布。

如果两个服务长期一起变更和一起发布，要重新评估边界，必要时合并或重划。
```

## 回答评分点

高分答案应该覆盖：

- 分布式单体是拆代码但未拆边界。
- 数据共享和联动发布是典型信号。
- 数据所有权和稳定契约是关键。
- 服务要能独立测试、部署和运行。
- 错误拆分可以合并或重划。
