# 229 如何保证事件 schema 兼容？

[返回按分类学习面试题](../README.md)

## 题目

如何保证事件 schema 兼容？

## 先给面试官的短答案

事件 schema 兼容要靠规则、注册表、兼容性检查、契约测试和灰度发布。
生产者不能随意删除字段、改字段类型或改变字段语义；消费者也要忽略未知字段，并兼容新旧版本一段时间。

事件一旦被多个服务订阅，就成为正式契约。

## 兼容规则

通常兼容：

- 新增可选字段。
- 新增消费者不依赖的字段。
- 新增枚举但消费者有默认处理。

通常不兼容：

- 删除字段。
- 修改字段类型。
- 改变字段含义。
- 把可选字段改为必填。
- 删除枚举值。

规则要在 CI 中自动检查。

## Schema Registry

可以使用 schema registry 管理：

- 事件名称。
- schema 版本。
- 兼容性策略。
- 生产者和消费者。
- 历史版本。

生产者发布新 schema 前先做兼容性校验。

## 消费者策略

消费者应：

- 忽略未知字段。
- 对缺失可选字段使用默认值。
- 明确不支持版本时进入死信。
- 不依赖未声明字段。
- 做契约测试。

兼容是生产者和消费者共同责任。

## 在 eMall 项目中怎么讲？

`OrderPaid` 事件新增 `paymentChannel` 字段通常兼容。

但把 `amountCents` 从数字改成字符串就是破坏性变更，会导致库存、财务和数据仓库消费失败。

## 深度增强：一致性和补偿图

![交易一致性、对账和补偿闭环](../assets/consistency-compensation-loop.svg)

分布式一致性题要先区分事实来源、状态流转和补偿责任。
订单、库存、支付、优惠和消息不可能总靠一个本地事务完成，
所以要用幂等、状态机、Outbox、重试、对账和补偿形成闭环。

## 深度增强：Java 17 状态机示例

```java
enum TradeState {
    INIT,
    RESERVED,
    PAID,
    CLOSED
}

record TradeTransition(TradeState from, TradeState to, String reason) {

    boolean valid() {
        return switch (from) {
            case INIT -> to == TradeState.RESERVED || to == TradeState.CLOSED;
            case RESERVED -> to == TradeState.PAID || to == TradeState.CLOSED;
            case PAID, CLOSED -> false;
        };
    }
}
```

状态机的价值是防止非法跳转。生产事故中很多错误不是技术异常，而是状态被重复推进、逆向推进或越级推进。

## 深度增强：生产边界

最终一致不是“最终随便一致”。每个异步环节都要有唯一业务键、幂等处理、重试策略、死信、补偿任务和对账报表。
涉及资金和库存时，宁可慢一点，也要保证事实可追踪、可审计、可修复。

## 深度增强：面试高分表达

我会先承认分布式系统无法用一个本地事务覆盖所有服务，再说明如何把不确定性收敛：
本地事务写事实和 Outbox，消费者幂等处理，失败进入重试和死信，后台对账发现差异并补偿。

## 专家级完整回答

```text
事件 schema 要按契约治理。新增可选字段通常兼容，删除字段、改类型、改语义、把可选改必填通常不兼容。
我会使用 schema registry、CI 兼容性检查和消费者契约测试，确保生产者变更不会破坏消费者。

消费者也要忽略未知字段，兼容新旧版本一段时间，并对不支持版本进入死信和告警。
```

## 回答评分点

高分答案应该覆盖：

- 事件 schema 是服务契约。
- 兼容和不兼容变更要区分。
- Schema Registry 能管理版本。
- CI 兼容性检查和契约测试重要。
- 消费者要忽略未知字段。
