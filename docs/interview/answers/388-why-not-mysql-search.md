# 388 为什么电商搜索通常不用 MySQL 直接实现？

[返回按分类学习面试题](../README.md)

## 先给面试官的短答案

电商搜索需要全文检索、分词、相关性排序、多条件过滤、聚合、召回、排序和高并发查询。MySQL 更
适合事务和结构化查询，不适合大规模复杂搜索和相关性计算。

所以通常使用 OpenSearch、Elasticsearch 或专门搜索引擎构建搜索读模型。

## 搜索需求

电商搜索需要：

- 关键词分词。
- 倒排索引。
- 多字段相关性。
- 类目和品牌过滤。
- 价格区间过滤。
- 销量和评分排序。
- 广告和运营干预。
- 高亮和纠错。

这些不是传统 OLTP 数据库的强项。

## MySQL 的限制

限制：

- `LIKE` 查询难以走高效索引。
- 多条件组合复杂。
- 相关性排序能力弱。
- 聚合和分页成本高。
- 高并发搜索会影响交易库。
- 分词和召回能力有限。

交易库不能被搜索流量拖垮。

## 搜索引擎优势

优势：

- 倒排索引。
- 分词器。
- 相关性打分。
- 分布式查询。
- 聚合统计。
- 读模型可重建。

搜索引擎牺牲部分实时一致性，换取搜索能力和吞吐。

## 使用边界与代价

只有主键、唯一键和少量结构化过滤时，MySQL 更简单，不应为了技术栈完整而引入 OpenSearch。
OpenSearch 会增加索引同步、映射演进、分片容量、集群运维、深分页和最终一致性成本。

商品库仍然保存权威事实，搜索索引必须能够通过全量重建和增量事件恢复；下单前还要回源校验上下架、价格和库存。

## 电商系统实践

大型电商系统商品库负责商品主数据和事务一致性，搜索服务维护商品搜索文档。

商品变更通过事件同步到搜索索引。搜索结果可能短暂滞后，但不会影响下单最终校验。
