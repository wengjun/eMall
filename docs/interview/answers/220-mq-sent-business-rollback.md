# 220 MQ 发送成功但业务事务回滚怎么办？

[返回按分类学习面试题](../README.md)

## 题目

MQ 发送成功但业务事务回滚怎么办？

## 先给面试官的短答案

这个问题通常来自先发 MQ、后提交业务事务，或者 MQ 发送不在事务边界内。
正确做法是不要在业务事务提交前让外部消费者看到消息，应使用 Outbox、事务消息或提交后可靠发布机制。

如果错误消息已经发出，消费者要通过状态校验和幂等避免错误处理，并由补偿事件修正。

## 问题场景

错误流程：

```text
发送 OrderCreated MQ
写订单表
提交事务失败
```

消费者收到消息后查询不到订单，或者基于不存在的订单执行后续动作。

## 推荐方案

使用 Outbox：

- 业务表和 outbox 表同事务提交。
- 事务回滚时 outbox 记录也回滚。
- 投递器只投递已提交的 outbox 记录。

这样不会出现业务回滚但事件已被消费者处理的问题。

## 消费者防护

消费者仍要防护：

- 查询业务状态确认事件有效。
- 处理不存在资源时延迟重试。
- 对非法状态拒绝处理。
- 记录异常事件并告警。

消息系统是至少一次投递，消费者不能无条件相信每条消息都能立即处理。

## 在 eMall 项目中怎么讲？

库存服务收到 `OrderCreated` 事件后，应查询订单状态或根据事件版本校验订单是否有效。

但根本方案仍是订单服务用 Outbox，确保只有订单事务提交后才发布事件。

## 深度增强：一致性和补偿图

![交易一致性、对账和补偿闭环](../assets/consistency-compensation-loop.svg)

分布式一致性题要先区分事实来源、状态流转和补偿责任。
订单、库存、支付、优惠和消息不可能总靠一个本地事务完成，
所以要用幂等、状态机、Outbox、重试、对账和补偿形成闭环。

## 深度增强：Java 17 状态机示例

```java
enum TradeState {
    INIT,
    RESERVED,
    PAID,
    CLOSED
}

record TradeTransition(TradeState from, TradeState to, String reason) {

    boolean valid() {
        return switch (from) {
            case INIT -> to == TradeState.RESERVED || to == TradeState.CLOSED;
            case RESERVED -> to == TradeState.PAID || to == TradeState.CLOSED;
            case PAID, CLOSED -> false;
        };
    }
}
```

状态机的价值是防止非法跳转。生产事故中很多错误不是技术异常，而是状态被重复推进、逆向推进或越级推进。

## 深度增强：生产边界

最终一致不是“最终随便一致”。每个异步环节都要有唯一业务键、幂等处理、重试策略、死信、补偿任务和对账报表。
涉及资金和库存时，宁可慢一点，也要保证事实可追踪、可审计、可修复。

## 深度增强：面试高分表达

我会先承认分布式系统无法用一个本地事务覆盖所有服务，再说明如何把不确定性收敛：
本地事务写事实和 Outbox，消费者幂等处理，失败进入重试和死信，后台对账发现差异并补偿。

## 专家级完整回答

```text
MQ 发送成功但业务事务回滚，根因是消息发布和业务提交不在同一个可靠边界内。
正确做法是使用 Outbox 或事务消息，业务事务回滚时待发送消息也回滚，只有已提交记录才会被投递。

如果错误消息已发出，消费者要通过状态校验、幂等和重试避免错误副作用，并通过补偿事件修正异常状态。
```

## 回答评分点

高分答案应该覆盖：

- 不应在业务事务提交前发布可见消息。
- Outbox 能避免回滚后消息仍发送。
- 消费者需要状态校验。
- 异常消息要告警和补偿。
- 根因是数据库和 MQ 边界不一致。
