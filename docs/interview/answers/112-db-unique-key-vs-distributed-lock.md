# 112 为什么数据库唯一键通常比分布式锁更可靠？

[返回按分类学习面试题](../README.md)

## 题目

为什么数据库唯一键通常比分布式锁更可靠？

## 先给面试官的短答案

数据库唯一键把并发约束放在最终写入资源的地方，由数据库原子保证，不依赖客户端是否正确加锁、锁是否过期或网络是否正常。
分布式锁只能降低并发冲突，不能替代最终一致性约束。

对防重复下单、优惠券领取、支付回调幂等这类场景，唯一键和幂等表通常比锁更可靠。

## 锁是外部协调

分布式锁在资源外部做协调。

风险包括：

- 锁过期。
- 误删锁。
- 续约失败。
- 网络分区。
- 客户端暂停。
- 代码忘记释放。

锁成功不代表后续写入一定正确。

## 唯一键是资源端约束

数据库唯一键直接作用在最终数据上。

例如：

```sql
unique(user_id, coupon_id)
```

无论多少实例同时插入，数据库只允许成功一条。

这是资源端原子约束。

## 幂等场景

支付回调可能重复到达。

可以设计：

```sql
unique(payment_no)
```

第一次处理成功，后续重复请求插入失败或查到已处理结果，直接返回成功。

这比先抢分布式锁再写库更可靠。

## 唯一键不是万能

唯一键适合防重复。

它不能解决所有问题，例如：

- 复杂跨表事务。
- 长流程互斥。
- 外部资源调用。
- 高并发热点更新性能。

但对于“最多只能有一条记录”的场景，它是最直接的正确性保障。

## 在 eMall 项目中怎么讲？

优惠券领取应使用唯一键防重复：

```text
user_id + coupon_id
```

订单创建可使用业务幂等号：

```text
client_request_id unique
```

分布式锁最多用于减少冲突，不作为唯一正确性保障。

## 深度增强：并发治理图

![Java 并发从线程安全到容量保护](../assets/concurrency-governance.svg)

并发题不能只回答 API 用法。生产系统要同时考虑线程安全、资源隔离、超时、拒绝、幂等和分布式多实例。
单机锁只能保护当前 JVM，不能保护整个集群；线程池满也不是小问题，而是容量和可用性风险。

## 深度增强：Java 17 有界并发示例

```java
import java.util.concurrent.Semaphore;
import java.util.function.Supplier;

final class BulkheadGuard {
    private final Semaphore permits;

    BulkheadGuard(int maxConcurrentCalls) {
        this.permits = new Semaphore(maxConcurrentCalls);
    }

    <T> T execute(Supplier<T> supplier) {
        if (!permits.tryAcquire()) {
            throw new IllegalStateException("Bulkhead rejected the call");
        }
        try {
            return supplier.get();
        } finally {
            permits.release();
        }
    }
}
```

这段代码展示了并发控制的生产思路：不是让所有请求无限进入系统，而是在入口保护共享资源。
真实项目还要加超时、指标、降级和按下游隔离。

## 深度增强：生产边界

线程安全不等于系统安全。`ConcurrentHashMap` 只能保护当前进程内的数据结构，
不能替代数据库唯一键、幂等表或分布式一致性。线程池也不能使用无界队列，
否则会把过载转化成内存上涨和 P99 恶化。

## 深度增强：面试高分表达

我会把并发问题分成三层：JMM 和锁保证单机正确性，线程池和隔离保证资源不被拖垮，
幂等和唯一键保证分布式正确性。这样能体现我理解 Java 并发，也理解微服务生产稳定性。

## 专家级完整回答

```text
数据库唯一键更可靠，是因为它把约束放在最终写入资源的地方，由数据库原子执行。
分布式锁是外部协调，受 TTL、网络、续约和客户端暂停影响，只能降低冲突概率。

对防重复领取、支付回调幂等、重复下单这类问题，我会优先设计唯一键、幂等表和状态机。
锁可以作为优化，但不能替代资源端约束。
```

## 回答评分点

高分答案应该覆盖：

- 唯一键是资源端原子约束。
- 分布式锁是外部协调。
- 锁有过期和网络风险。
- 幂等场景优先唯一键。
- 唯一键不是万能但很可靠。
