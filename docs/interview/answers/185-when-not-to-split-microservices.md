# 185 什么情况下不应该拆微服务？

[返回按分类学习面试题](../README.md)

## 题目

什么情况下不应该拆微服务？

## 先给面试官的短答案

边界不清、团队规模小、业务快速试错、数据强耦合、缺少自动化测试和运维能力时，不应该急着拆微服务。
此时模块化单体通常更稳，能保留清晰边界，同时避免分布式系统复杂度。

微服务不是起点，很多时候是规模增长后的治理手段。

## 不适合拆分的信号

信号包括：

- 业务模型还在频繁变化。
- 服务边界没有达成共识。
- 多个模块必须共享同一事务。
- 团队没有独立运维能力。
- 缺少 CI/CD、监控和追踪。
- 测试体系薄弱。
- 系统流量和团队规模都不大。

这些情况下拆分会让开发速度变慢。

## 过早拆分的后果

后果包括：

- 一个需求要改多个服务。
- 本地开发和联调困难。
- 接口频繁变更。
- 数据一致性问题增加。
- 线上排障成本大幅上升。
- 服务数量增长但业务价值没有增长。

这就是典型的架构复杂度提前透支。

## 更好的做法

可以先做模块化单体：

- 代码按领域分包。
- 模块之间只通过接口调用。
- 数据访问边界清晰。
- 禁止跨模块随意访问表。
- 保留未来拆分的可能。

这样既能控制复杂度，又能训练服务边界。

## 在 eMall 项目中怎么讲？

如果早期只有少量用户和一个小团队，订单、库存、支付可以先在一个部署单元中保持清晰模块边界。

当流量、团队、发布节奏和故障隔离需求增长后，再把成熟边界拆成独立服务。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
不应该在边界不清、业务快速变化、团队规模小、数据强耦合和运维能力不足时拆微服务。
这时拆分会把本来可以通过模块边界解决的问题，升级成网络、事务、监控和发布问题。

我更倾向于先做模块化单体，明确领域边界和数据访问规则，等边界稳定、团队和流量增长后再拆。
```

## 回答评分点

高分答案应该覆盖：

- 微服务不适合所有阶段。
- 边界不清时不要拆。
- 运维和测试能力不足时风险很高。
- 模块化单体是合理中间形态。
- 拆分要等收益大于成本。
