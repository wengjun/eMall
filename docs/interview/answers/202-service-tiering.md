# 202 如何做服务分级？

[返回按分类学习面试题](../README.md)

## 题目

如何做服务分级？

## 先给面试官的短答案

服务分级是按业务重要性、故障影响、收入影响、用户影响和依赖程度给服务划分等级。
分级后，不同等级服务使用不同的 SLA、容量冗余、发布门禁、告警策略、应急预案和演练频率。

没有分级，资源会平均分配，真正关键的链路得不到足够保护。

## 常见分级

可以划分为：

- P0：全站核心交易和登录支付链路。
- P1：重要业务能力，故障明显影响用户体验。
- P2：普通业务能力，可短时间降级。
- P3：后台、离线、辅助系统。

具体等级应结合公司业务定义。

## 分级依据

依据包括：

- 是否影响下单和支付。
- 是否影响登录和鉴权。
- 是否影响资金和库存正确性。
- 是否影响大面积用户。
- 是否被多个核心服务依赖。
- 是否有替代或降级方案。

分级要定期复核，业务变化后服务等级也会变化。

## 分级后的治理

治理差异：

- P0 服务需要更高可用性和容量冗余。
- P0 发布要更严格灰度和回滚。
- P0 告警要更低延迟。
- P2/P3 可以接受更长恢复时间。
- 非核心服务要能被主链路降级。

分级要落到流程和资源上。

## 在 eMall 项目中怎么讲？

订单、库存、支付、用户鉴权属于 P0 或 P1。

推荐、广告、报表、数据仓库通常不应阻塞交易主链路，可以按 P2/P3 管理，并提供降级策略。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
服务分级是根据业务影响、用户影响、收入影响、数据正确性和依赖程度划分服务重要性。
分级后要落到 SLA、容量、告警、发布、变更审批、演练和应急预案上。

在电商中，订单、库存、支付、鉴权属于高等级服务；推荐、广告、报表可以降级，治理要求和核心交易服务不同。
```

## 回答评分点

高分答案应该覆盖：

- 分级依据是业务影响和故障半径。
- P0/P1/P2/P3 治理要求不同。
- 分级后要影响容量、发布和告警。
- 服务等级要定期复核。
- 非核心服务不能阻塞核心链路。
