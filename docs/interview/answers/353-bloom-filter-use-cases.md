# 353 布隆过滤器适合什么场景？

[返回按分类学习面试题](../README.md)

## 题目

布隆过滤器适合什么场景？

## 先给面试官的短答案

布隆过滤器适合判断某个元素是否可能存在，尤其适合大规模集合的快速预判，例如商品 ID 是否存在、
用户是否可能存在、爬虫 URL 去重和缓存穿透防护。

它的特点是可能误判存在，但不会漏判已加入的数据。

## 工作原理

布隆过滤器由位数组和多个哈希函数组成。

加入元素时，多个哈希函数计算多个位置，并把这些位置置为 1。查询元素时，如果任意位置为 0，
说明一定不存在。如果所有位置都是 1，说明可能存在。

## 适合场景

适合：

- 缓存穿透防护。
- 大规模 ID 存在性判断。
- URL 去重。
- 黑名单预过滤。
- 推荐去重初筛。

这些场景能接受少量误判。

## 不适合场景

不适合：

- 需要精确判断不存在误判的场景。
- 需要频繁删除的场景。
- 数据量远超设计容量的场景。
- 误判会造成资金或权益错误的场景。

误判率和容量要提前估算。

## 在 eMall 项目中怎么讲？

eMall 可以把已上架商品 ID 加入布隆过滤器。商品详情请求先查布隆过滤器，如果判断一定不存在，
直接返回不存在或访问空值缓存，避免打到数据库。

如果布隆过滤器判断可能存在，再查 Redis 和数据库。误判最多多查一次数据库，不会把存在商品过滤掉。

## 深度增强：缓存和消息治理图

![数据库、缓存和消息一致性链路](../assets/data-cache-mq.svg)

缓存和消息题要关注一致性、削峰、延迟、积压和恢复。
Redis 很快，但会遇到穿透、击穿、雪崩、热点 key 和内存淘汰；
MQ 能解耦和削峰，但会带来重复消费、乱序、积压和死信处理。

## 深度增强：Java 17 幂等消费示例

```java
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

final class LocalIdempotentConsumer {
    private final Set<String> processedKeys = ConcurrentHashMap.newKeySet();

    boolean tryHandle(String messageKey, Runnable handler) {
        if (!processedKeys.add(messageKey)) {
            return false;
        }
        handler.run();
        return true;
    }
}
```

这个示例只适合解释幂等思想。生产环境不能用本地内存做全局幂等，要使用数据库唯一键、Redis 原子操作或业务状态机。

## 深度增强：生产边界

缓存要有 TTL、容量、降级和回源保护；消息要有重试、死信、延迟队列、消费幂等和积压告警。
缓存不一致要能修复，消息失败要能回放，不能只依赖人工查日志。

## 深度增强：面试高分表达

我会把缓存和消息都看成性能与稳定性工具，而不是正确性事实来源。
正确性由数据库事实、状态机、幂等和对账保证；缓存和 MQ 负责降低延迟、削峰填谷和解耦系统。

## 专家级完整回答

```text
布隆过滤器适合大规模存在性预判。它用位数组和多个哈希函数表示集合，查询时如果有任意位为 0，
元素一定不存在；如果都为 1，则只能说明可能存在。

它适合缓存穿透、商品 ID 预判和 URL 去重，但不适合要求绝对精确或频繁删除的场景。生产中要
设计容量和误判率，并准备重建机制。
```

## 回答评分点

高分答案应该覆盖：

- 可能误判存在。
- 不会漏判已加入数据。
- 位数组和多个哈希函数。
- 适合缓存穿透。
- 要评估容量和误判率。
