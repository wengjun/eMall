# 139 阻塞式和响应式调用如何取舍？

[返回按分类学习面试题](../README.md)

## 题目

阻塞式和响应式调用如何取舍？

## 先给面试官的短答案

阻塞式调用模型简单，适合大多数 CRUD、交易和团队熟悉 MVC 的场景；响应式调用适合高并发 IO、
长连接、流式处理和端到端非阻塞链路。响应式不是天然更快，它要求数据库、缓存、HTTP 客户端和业务代码都尽量非阻塞。

如果中间大量 `.block()`，响应式收益会明显降低。

## 阻塞式模型

阻塞式模型通常是一请求一线程。

优点：

- 编程简单。
- 调试容易。
- 生态成熟。
- 团队学习成本低。

缺点：

- 大量慢 IO 会占用线程。
- 线程数过多有上下文切换和内存成本。

适合大多数同步业务。

## 响应式模型

响应式模型使用少量线程处理大量 IO 事件。

优点：

- 非阻塞 IO 资源利用率高。
- 适合长连接和流式场景。
- 背压模型更清晰。

缺点：

- 编程复杂。
- 调试栈复杂。
- 阻塞调用会破坏模型。
- 团队学习成本高。

## 选择标准

选择响应式前要确认：

- 是否端到端非阻塞。
- 数据库驱动是否支持响应式。
- 下游客户端是否非阻塞。
- 团队是否熟悉 Reactor。
- 是否有高并发长 IO 场景。
- 可观测和排障是否准备好。

否则 MVC 可能更稳。

## 交易链路考虑

订单、支付、库存这类交易链路通常更关注一致性、可读性和故障处理。

阻塞式 MVC 加合理线程池、超时、熔断和限流，往往足够稳定。

不要为了响应式增加复杂度，除非收益明确。

## 在 eMall 项目中怎么讲？

eMall 网关、消息推送、实时库存订阅可能适合响应式。

订单创建、支付回调、库存扣减更适合同步模型和清晰事务边界。

混合架构可以存在，但每条链路内部要统一模型。

## 深度增强：Spring 服务治理图

![Spring 微服务调用栈和治理边界](../assets/spring-service-stack.svg)

Spring 题要从框架机制讲到业务边界。Controller 负责协议适配，Service 负责业务事务，Repository 负责数据访问；
事务、AOP、校验、错误码、配置和观测都是为了让微服务在复杂调用中保持稳定。

## 深度增强：Java 17 分层示例

```java
record CreateOrderCommand(long userId, long skuId, int quantity) {
}

record CreateOrderResult(long orderId, String status) {
}

interface OrderApplicationService {
    CreateOrderResult create(CreateOrderCommand command);
}

final class OrderControllerAdapter {
    private final OrderApplicationService service;

    OrderControllerAdapter(OrderApplicationService service) {
        this.service = service;
    }

    CreateOrderResult submit(CreateOrderCommand command) {
        return service.create(command);
    }
}
```

这个示例表达分层边界：接口层不堆业务逻辑，业务层不依赖 Web 协议，命令和结果对象形成稳定契约。

## 深度增强：生产边界

框架默认值不能替代设计。事务传播、异常回滚、异步线程池、连接池、序列化、超时和重试都要显式治理。
尤其在订单、支付、库存链路中，要避免长事务、隐式重试和跨服务事务误用。

## 深度增强：面试高分表达

我会先解释框架原理，再说明在电商系统里怎么落地。高分回答要能把自动配置、AOP、事务、MVC、WebFlux、
校验和错误处理，连接到可维护性、可观测性、稳定性和故障恢复。

## 专家级完整回答

```text
阻塞式模型简单成熟，适合大多数交易 CRUD；响应式适合高并发 IO、长连接、流式和端到端非阻塞链路。
响应式不是天然更快，如果数据库、缓存或业务代码仍然阻塞，频繁 block 会抵消收益并增加复杂度。

我会按链路特征选择：网关和推送可响应式，订单支付库存优先清晰同步模型和可靠事务边界。
```

## 回答评分点

高分答案应该覆盖：

- 阻塞式简单成熟。
- 响应式适合高并发 IO 和流式。
- 响应式要求端到端非阻塞。
- `.block()` 会破坏收益。
- 按业务链路选择。

## 深度完善：面向 L6 的回答框架

围绕「阻塞式和响应式调用如何取舍？」，高分答案不能停在概念定义，而要把「Bean 生命周期、AOP、事务、配置、HTTP 客户端、健康检查和公共配置」讲成一条可验证的工程链路。
面试官真正关注的是：你是否知道它解决什么问题、什么时候会失效、如何在生产系统中验证。

### 1. 先界定边界

- 本题属于「Spring Boot 和服务工程」，先说明它影响的是正确性、稳定性、性能、安全还是协作效率。
- 不要直接背结论，要先说清业务约束、数据规模、调用链位置和失败后果。
- 如果存在多种方案，要说明默认选择、替代方案、迁移成本和放弃条件。

### 2. 结合 eMall 落地

- 可以从 `各服务的 Controller、ApplicationService、MyBatis Plus Mapper、Actuator 和 RestClient` 切入，说明它在真实电商链路中的入口、状态、数据和依赖。
- 回答时至少补一个失败路径，例如超时、重复请求、状态不一致、热点流量或配置误发。
- 再说明如何通过代码规范、测试、灰度、回滚、监控或补偿把风险收敛。

### 3. 生产级验证

- 关键指标：事务失败率、健康检查状态、依赖调用耗时、配置变更次数、启动耗时。
- 验证证据：Spring Boot 测试、集成测试、配置审计、Actuator 指标和链路 Trace。
- 如果没有这些证据，只能说明方案在理论上成立，不能证明它能长期稳定运行。

### 4. 追问防守

- 被问“为什么不用更简单方案”时，回答当前规模、团队能力和风险收益是否匹配。
- 被问“为什么不用更复杂方案”时，回答复杂方案的运维成本、故障面和迁移成本。
- 最后用一句话收束：先用简单可靠方案闭环，再用指标驱动演进，而不是提前复杂化。

## 补强索引
本题复习重点：阻塞式和响应式调用如何取舍？

- 先看本文的题目专属答案，再按共享框架补齐项目落点、失败路径、取舍和验收。
- 白板复述时用结论 -> 例子 -> 风险 -> 指标四层结构。
