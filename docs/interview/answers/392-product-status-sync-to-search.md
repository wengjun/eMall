# 392 商品上下架如何同步到搜索索引？

[返回按分类学习面试题](../README.md)

## 题目

商品上下架如何同步到搜索索引？

## 先给面试官的短答案

商品上下架应由商品服务在状态变更后发布领域事件，搜索服务消费事件并更新或删除搜索索引。
上下架属于用户强感知状态，必须有重试、幂等、监控和补偿校验。

搜索索引可以短暂延迟，但不能长期展示已下架商品。

## 同步流程

流程：

- 商品服务更新上下架状态。
- 同事务写 Outbox。
- Relay 发布商品变更事件。
- 搜索服务消费事件。
- 更新搜索文档状态或删除文档。
- 记录处理结果和 offset。

事件发布和数据库变更要最终一致。

## 更新还是删除

两种方式：

- 下架时删除搜索文档。
- 下架时保留文档但设置 `status=OFF_SHELF`。

保留状态字段方便审计和快速恢复，但查询必须过滤下架状态。

## 补偿机制

补偿包括：

- 定时对比商品库和搜索索引。
- 发现不一致后重建单商品索引。
- 失败事件进入重试和死信。
- 下架事件高优先级处理。
- 搜索结果点击前再次校验状态。

补偿是搜索最终一致性的关键。

## 在 eMall 项目中怎么讲？

eMall 商品下架后，搜索服务应尽快把搜索文档标记为不可搜索或删除。

如果搜索索引短暂滞后，用户点击商品详情时，商品服务仍要按主库状态返回已下架，不能继续允许下单。

## 深度增强：缓存和消息治理图

![数据库、缓存和消息一致性链路](../assets/data-cache-mq.svg)

缓存和消息题要关注一致性、削峰、延迟、积压和恢复。
Redis 很快，但会遇到穿透、击穿、雪崩、热点 key 和内存淘汰；
MQ 能解耦和削峰，但会带来重复消费、乱序、积压和死信处理。

## 深度增强：Java 17 幂等消费示例

```java
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

final class LocalIdempotentConsumer {
    private final Set<String> processedKeys = ConcurrentHashMap.newKeySet();

    boolean tryHandle(String messageKey, Runnable handler) {
        if (!processedKeys.add(messageKey)) {
            return false;
        }
        handler.run();
        return true;
    }
}
```

这个示例只适合解释幂等思想。生产环境不能用本地内存做全局幂等，要使用数据库唯一键、Redis 原子操作或业务状态机。

## 深度增强：生产边界

缓存要有 TTL、容量、降级和回源保护；消息要有重试、死信、延迟队列、消费幂等和积压告警。
缓存不一致要能修复，消息失败要能回放，不能只依赖人工查日志。

## 深度增强：面试高分表达

我会把缓存和消息都看成性能与稳定性工具，而不是正确性事实来源。
正确性由数据库事实、状态机、幂等和对账保证；缓存和 MQ 负责降低延迟、削峰填谷和解耦系统。

## 专家级完整回答

```text
商品上下架同步通常通过事件驱动。商品服务在本地事务中更新状态并写 Outbox，搜索服务消费事件后
更新搜索索引。下架可以删除文档，也可以保留文档并设置不可搜索状态。

因为搜索索引是最终一致读模型，所以必须有重试、死信、定时对账和单商品重建能力。交易侧仍以
商品主库状态为准。
```

## 回答评分点

高分答案应该覆盖：

- 商品状态变更发布事件。
- Outbox 保证事件不丢。
- 搜索服务幂等更新索引。
- 下架要高优先级和补偿。
- 下单仍校验商品主库状态。
