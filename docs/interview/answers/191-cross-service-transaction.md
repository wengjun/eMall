# 191 跨服务事务怎么处理？

[返回按分类学习面试题](../README.md)

## 题目

跨服务事务怎么处理？

## 先给面试官的短答案

大型微服务系统通常避免全局大事务，优先使用本地事务、状态机、可靠消息、幂等、补偿和对账来实现最终一致。
只有在强一致收益明显且规模可控的场景，才考虑 TCC 或 2PC。

跨服务事务的核心不是“让所有服务同时提交”，而是让业务状态可恢复、可补偿、可验证。

## 为什么不用全局事务？

全局事务问题包括：

- 锁持有时间长。
- 参与者故障会阻塞。
- 网络抖动影响提交结果。
- 吞吐量低。
- 跨团队服务耦合严重。

电商下单、支付、库存这些链路流量大，不能把所有服务绑在一个长事务里。

## 常见方案

常见方案：

- 本地事务加 Outbox。
- Saga 编排或协同。
- TCC 预留、确认、取消。
- 状态机驱动补偿。
- 定时对账和修复。

选择取决于业务是否可补偿、是否需要资源预留、延迟是否可接受。

## 关键保障

必须具备：

- 幂等。
- 去重。
- 状态机。
- 可靠消息。
- 失败重试。
- 死信恢复。
- 对账任务。
- 操作审计。

没有这些保障，最终一致只是口号。

## 在 eMall 项目中怎么讲？

下单时订单服务先创建待支付订单，库存服务做库存预占，支付成功后订单状态变为已支付。

如果库存预占失败，订单进入失败或取消状态；如果支付成功但订单更新失败，通过支付事件重试和对账任务修复。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
跨服务事务通常不用全局大事务，而是用本地事务、可靠消息、状态机、幂等、补偿和对账实现最终一致。
订单、库存、支付各自提交本地事务，通过事件推动状态前进，失败时进入可恢复状态并由补偿任务修复。

如果业务需要强资源预留，可以考虑 TCC；如果只是流程型长事务，可以考虑 Saga。方案选择要看一致性、吞吐和补偿成本。
```

## 回答评分点

高分答案应该覆盖：

- 大型系统通常避免全局大事务。
- 本地事务和可靠消息是基础。
- Saga、TCC 适用场景不同。
- 幂等、状态机、补偿和对账不可缺少。
- 业务状态要可恢复。
