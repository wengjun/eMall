# 372 事件应该携带全量数据还是只携带 ID？

[返回按分类学习面试题](../README.md)

## 题目

事件应该携带全量数据还是只携带 ID？

## 先给面试官的短答案

事件携带全量数据还是只携带 ID，要看事件用途、一致性要求、消息大小、消费者自治能力和数据隐私。
全量数据能降低消费者回查依赖，只携带 ID 能减小消息并避免敏感数据扩散。

生产中常用折中方案：事件携带关键业务快照和 ID，不携带过大的明细或敏感字段。

## 只携带 ID

优点：

- 消息小。
- schema 简单。
- 敏感数据扩散少。
- 数据以源服务最新状态为准。

缺点：

- 消费者需要回查源服务。
- 源服务压力增加。
- 回查可能读到事件之后的新状态。
- 源服务故障会影响消费。

## 携带全量数据

优点：

- 消费者自治。
- 减少回查。
- 保留事件发生时快照。
- 适合构建读模型。

缺点：

- 消息变大。
- schema 演进复杂。
- 敏感数据风险增加。
- 重复存储成本高。

全量也不应该无限制携带大字段。

## 推荐折中

推荐携带：

- 事件 ID。
- 聚合根 ID。
- 事件类型。
- 发生时间。
- 版本号。
- 关键业务字段快照。
- 追踪 ID。

大文本、图片、长列表和敏感字段尽量不放。

## 在 eMall 项目中怎么讲？

eMall 的订单创建事件可以携带 `orderId`、`userId`、金额、订单状态、商品行摘要和事件版本。

不要把完整商品描述、用户隐私信息和大促复杂规则全部塞进消息。履约和搜索等消费者能基于快照
处理，必要时再按 ID 回查。

## 深度增强：缓存和消息治理图

![数据库、缓存和消息一致性链路](../assets/data-cache-mq.svg)

缓存和消息题要关注一致性、削峰、延迟、积压和恢复。
Redis 很快，但会遇到穿透、击穿、雪崩、热点 key 和内存淘汰；
MQ 能解耦和削峰，但会带来重复消费、乱序、积压和死信处理。

## 深度增强：Java 17 幂等消费示例

```java
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;

final class LocalIdempotentConsumer {
    private final Set<String> processedKeys = ConcurrentHashMap.newKeySet();

    boolean tryHandle(String messageKey, Runnable handler) {
        if (!processedKeys.add(messageKey)) {
            return false;
        }
        handler.run();
        return true;
    }
}
```

这个示例只适合解释幂等思想。生产环境不能用本地内存做全局幂等，要使用数据库唯一键、Redis 原子操作或业务状态机。

## 深度增强：生产边界

缓存要有 TTL、容量、降级和回源保护；消息要有重试、死信、延迟队列、消费幂等和积压告警。
缓存不一致要能修复，消息失败要能回放，不能只依赖人工查日志。

## 深度增强：面试高分表达

我会把缓存和消息都看成性能与稳定性工具，而不是正确性事实来源。
正确性由数据库事实、状态机、幂等和对账保证；缓存和 MQ 负责降低延迟、削峰填谷和解耦系统。

## 专家级完整回答

```text
事件携带全量还是 ID 是自治和成本的权衡。只携带 ID 消息小但消费者依赖回查；全量快照减少回查，
但消息大、schema 演进复杂且有隐私风险。

生产中通常携带事件 ID、聚合根 ID、事件类型、发生时间、版本和关键业务快照。大字段和敏感字段
不要放入事件，必要时消费者再回查。
```

## 回答评分点

高分答案应该覆盖：

- 说明两种方式的优缺点。
- 全量快照降低回查。
- ID 模式消息小但依赖源服务。
- 推荐关键快照加 ID。
- 注意敏感数据和消息大小。
