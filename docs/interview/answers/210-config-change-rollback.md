# 210 如何处理配置变更回滚？

[返回按分类学习面试题](../README.md)

## 题目

如何处理配置变更回滚？

## 先给面试官的短答案

配置回滚要像代码回滚一样严肃。配置中心需要版本历史、变更审计、影响面分析、灰度发布、快速回滚和回滚后验证。
高风险配置不能直接全量发布，必须先小范围验证。

回滚不是把值改回去那么简单，还要确认所有实例都生效并且业务指标恢复。

## 回滚前提

需要具备：

- 配置版本号。
- 变更人和审批记录。
- 变更原因。
- 差异对比。
- 影响服务列表。
- 发布批次。
- 实例生效状态。

没有版本和审计，事故时很难确定该回滚到哪里。

## 回滚流程

推荐流程：

- 发现异常并确认关联配置变更。
- 暂停继续放量。
- 回滚到上一稳定版本。
- 观察实例配置版本。
- 验证错误率、延迟和业务指标。
- 记录事故和改进措施。

如果配置和代码版本有关，还要确认旧配置是否兼容当前代码。

## 风险点

风险包括：

- 部分实例未收到回滚配置。
- 配置缓存未刷新。
- 回滚配置与新代码不兼容。
- 多个配置同时变更，难以定位。
- 回滚后业务状态已经产生副作用。

所以高风险配置要避免一次改太多。

## 在 eMall 项目中怎么讲？

如果秒杀限流阈值误改导致大量正常用户被限流，应立即回滚到上一稳定版本。

回滚后还要检查所有秒杀实例是否加载同一配置版本，并观察下单成功率、限流率和错误率。

## 深度增强：可观测与配置治理图

![指标、日志、Trace 和告警平台](../assets/observability-platform.svg)

配置、日志、指标和 Trace 不是附属能力，而是生产系统定位问题和控制变更风险的基础。
没有可观测性，限流、熔断、回滚和补偿都很难判断是否有效。

## 深度增强：Java 17 观测信号示例

```java
import java.time.Instant;
import java.util.Map;

record ObservabilityEvent(
        Instant time,
        String traceId,
        String service,
        String eventType,
        Map<String, String> tags) {
}

final class TraceTagPolicy {

    boolean shouldKeep(String key) {
        return !key.equalsIgnoreCase("password")
                && !key.equalsIgnoreCase("secret")
                && !key.equalsIgnoreCase("token");
    }
}
```

这段代码体现生产观测的两个重点：所有关键事件要能关联 traceId，敏感信息不能进入日志和标签。

## 深度增强：生产边界

日志越多不代表越好。核心链路要控制日志成本、采样率、脱敏和索引字段。告警也不能只看机器指标，
还要看下单成功率、支付成功率、库存失败率、Outbox 积压和用户投诉。

## 深度增强：面试高分表达

我会把可观测性讲成故障闭环：指标发现异常，Trace 定位慢在哪里，日志解释发生了什么，
告警和 Runbook 指导恢复。配置变更也要有版本、审批、灰度、审计和回滚，避免配置事故变成全站事故。

## 专家级完整回答

```text
配置回滚要依赖版本化、审计、灰度和实例生效状态。出现异常后先停止放量，再回滚到上一稳定版本，
并确认所有实例都加载了回滚配置。

回滚后要观察技术指标和业务指标。如果配置与代码版本有关，还要验证兼容性。高风险配置应小步变更，避免一次修改过多项。
```

## 回答评分点

高分答案应该覆盖：

- 配置必须版本化和可审计。
- 回滚要确认实例生效状态。
- 回滚后要验证技术和业务指标。
- 配置与代码可能存在兼容性。
- 高风险配置应灰度发布。
