apiVersion: apps/v1
kind: Deployment
metadata:
  name: openapi
  labels:
    app: openapi
spec:
  replicas: 4
  selector:
    matchLabels:
      app: openapi
  template:
    metadata:
      labels:
        app: openapi
    spec:
      terminationGracePeriodSeconds: 45
      serviceAccountName: openapi
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: openapi
          image: emall/openapi:0.1.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8100
          envFrom:
            - configMapRef:
                name: emall-runtime-config
            - secretRef:
                name: emall-runtime-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8100
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8100
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
---
apiVersion: v1
kind: Service
metadata:
  name: openapi
spec:
  selector:
    app: openapi
  ports:
    - name: http
      port: 8100
      targetPort: 8100
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: openapi
spec:
  minAvailable: 3
  selector:
    matchLabels:
      app: openapi
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: openapi
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: openapi
  minReplicas: 4
  maxReplicas: 200
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
