apiVersion: apps/v1
kind: Deployment
metadata:
  name: cost
  labels:
    app: cost
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cost
  template:
    metadata:
      labels:
        app: cost
    spec:
      terminationGracePeriodSeconds: 45
      serviceAccountName: cost
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cost
          image: emall/cost:0.1.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8096
          envFrom:
            - configMapRef:
                name: emall-runtime-config
            - secretRef:
                name: emall-runtime-secret
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 15"]
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8096
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8096
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: cost
spec:
  selector:
    app: cost
  ports:
    - name: http
      port: 8096
      targetPort: 8096
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cost
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: cost
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cost
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cost
  minReplicas: 3
  maxReplicas: 80
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
