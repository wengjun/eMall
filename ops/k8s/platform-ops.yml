apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-ops
  labels:
    app: platform-ops
spec:
  replicas: 3
  selector:
    matchLabels:
      app: platform-ops
  template:
    metadata:
      labels:
        app: platform-ops
    spec:
      terminationGracePeriodSeconds: 45
      serviceAccountName: platform-ops
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: platform-ops
          image: emall/platform-ops:0.1.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8116
          envFrom:
            - configMapRef:
                name: emall-runtime-config
            - secretRef:
                name: emall-runtime-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8116
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8116
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: platform-ops
spec:
  selector:
    app: platform-ops
  ports:
    - name: http
      port: 8116
      targetPort: 8116
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: platform-ops
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: platform-ops
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: platform-ops
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: platform-ops
  minReplicas: 3
  maxReplicas: 80
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
