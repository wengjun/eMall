apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant
  labels:
    app: merchant
spec:
  replicas: 3
  selector:
    matchLabels:
      app: merchant
  template:
    metadata:
      labels:
        app: merchant
    spec:
      terminationGracePeriodSeconds: 45
      serviceAccountName: merchant
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: merchant
          image: emall/merchant:0.1.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8093
          envFrom:
            - configMapRef:
                name: emall-runtime-config
            - secretRef:
                name: emall-runtime-secret
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 15"]
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8093
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8093
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: "500m"
              memory: 768Mi
            limits:
              cpu: "2"
              memory: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: merchant
spec:
  selector:
    app: merchant
  ports:
    - name: http
      port: 8093
      targetPort: 8093
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: merchant
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: merchant
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: merchant
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: merchant
  minReplicas: 3
  maxReplicas: 120
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
