apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk
  labels:
    app: risk
spec:
  replicas: 4
  selector:
    matchLabels:
      app: risk
  template:
    metadata:
      labels:
        app: risk
    spec:
      terminationGracePeriodSeconds: 45
      serviceAccountName: risk
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: risk
          image: emall/risk:0.1.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8098
          envFrom:
            - configMapRef:
                name: emall-runtime-config
            - secretRef:
                name: emall-runtime-secret
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8098
            initialDelaySeconds: 20
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8098
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
---
apiVersion: v1
kind: Service
metadata:
  name: risk
spec:
  selector:
    app: risk
  ports:
    - name: http
      port: 8098
      targetPort: 8098
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: risk
spec:
  minAvailable: 3
  selector:
    matchLabels:
      app: risk
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: risk
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: risk
  minReplicas: 4
  maxReplicas: 200
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
